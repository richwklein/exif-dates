[project]
name = "exif-dates"
version = "0.1.1"
requires-python = ">=3.12"

[project.optional-dependencies]
dev = ["pytest>=9.0.3", "ruff>=0.9"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.ruff.format]
quote-style = "double"
